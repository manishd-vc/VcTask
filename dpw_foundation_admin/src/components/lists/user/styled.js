import MenuList from '@mui/material/MenuList';
import { styled } from '@mui/material/styles';

const RootStyled = styled(MenuList)(() => ({
  paddingTop: 0,
  paddingBottom: 0
}));
export default RootStyled;
