// component
import LoginForm from 'src/components/forms/login';

/**
 * LoginMain - A component that renders the login form.
 *
 * @returns {JSX.Element} - The component that renders the login form.
 */
export default function LoginMain() {
  return <LoginForm />;
}
