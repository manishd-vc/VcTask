/**
 * EmailPreview component for rendering a preview of the email content.
 * Currently logs the form values to the console and renders an empty Box.
 *
 * @param {Object} props - The props for the component.
 * @param {Object} props.values - The form values passed from the parent component.
 *
 * @returns {JSX.Element} The rendered EmailPreview component.
 */
'use client';
import { Box } from '@mui/material';

export default function EmailPreview() {
  // Return an empty Box element (UI to be implemented later)
  return <Box></Box>;
}
