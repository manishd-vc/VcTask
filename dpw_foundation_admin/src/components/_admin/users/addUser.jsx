// components
import UserForm from 'src/components/forms/user';

/**
 * addUser Component - Renders the UserForm component for adding a new user.
 *
 * @returns {JSX.Element} - The rendered UserForm component.
 */
export default function addUser() {
  return <UserForm />;
}
