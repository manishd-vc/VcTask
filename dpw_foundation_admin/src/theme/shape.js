/**
 * Shape configuration for border radius values.
 * @type {Object}
 * @property {number} borderRadius - Default border radius for general use.
 * @property {number} borderRadiusSm - Border radius for small screens or components.
 * @property {number} borderRadiusMd - Border radius for medium screens or components.
 * @property {number} borderRadiusNo - No border radius (0) for flat components or elements.
 */
const shape = {
  borderRadius: 8, // Default border radius value for general use
  borderRadiusSm: 12, // Border radius for small screens or elements
  borderRadiusMd: 16, // Border radius for medium screens or elements
  borderRadiusNo: 0 // No border radius (0) for flat elements
};

export default shape;
