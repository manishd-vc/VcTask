const breakpoints = {
  values: {
    xs: 0, // Extra small screens, typically mobile devices
    sm: 600, // Small screens, typically tablets
    md: 900, // Medium screens, used as the previous breakpoint for tablets (old value was 960)
    lg: 1200, // Large screens, commonly for laptops/desktops (old value was 1280)
    xl: 1536 // Extra large screens, typically for large desktops (old value was 1920)
  }
};

export default breakpoints;
