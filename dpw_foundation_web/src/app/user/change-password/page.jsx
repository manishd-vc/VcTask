// mui

// components
import AccountChangePassword from 'src/components/_main/profile/edit/accountChangePassword';

/**
 * Page Component for Changing Password
 *
 * This page renders the `AccountChangePassword` component, which likely handles the logic and UI for changing a user's password.
 * The component is used here to display the password change form or interface.
 */
export default function page() {
  return <AccountChangePassword />;
}
